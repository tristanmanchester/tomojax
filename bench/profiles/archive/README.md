Archived benchmark profiles live here to keep `bench/profiles/` focused on the
active controller workflow documented in `bench/README.md`.

These profiles are retained for reference and ad hoc manual experiments, but
they are not part of the supported `smoke`, `screening`, or `canary` suites.
Do not add exact copies of active profiles here; Git history already preserves
old active-profile versions.
