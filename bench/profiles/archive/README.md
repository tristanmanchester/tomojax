Archived benchmark profiles live here to keep `bench/profiles/` focused on the active controller
workflow.

These profiles are retained for reference and ad hoc manual experiments, but they are not part of
the supported `smoke`, `screening`, or `canary` suites in
`tomojax-controller/targets/tomojax.yaml`.
